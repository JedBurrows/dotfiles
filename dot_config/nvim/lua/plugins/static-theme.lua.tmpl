-- Static colorscheme for non-omarchy machines (e.g. WSL).
-- Mirrors the omarchy gruvbox default; gruvbox.nvim is installed via all-themes.lua.
-- On omarchy/desktop this file is not applied (the live theme.lua symlink is used instead).
return {
  { "LazyVim/LazyVim", opts = { colorscheme = "gruvbox" } },
}
